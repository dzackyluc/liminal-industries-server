services:
  minecraft:
    build: .
    ports:
      - "25565:25565"
    volumes:
      - liminal-data:/data
    environment:
      - JAVA_OPTS=-Xmx6G -Xms4G
    restart: unless-stopped

volumes:
  liminal-data:
